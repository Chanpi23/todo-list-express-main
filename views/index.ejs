<!DOCTYPE html>
<!-- this is what langue we using -->
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <!--- add in our font awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <!-- link to css -->
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body> we gotz and extra one -->
    <h1>Todo List: </h1>
    <!-- most important item on page-->
    <ul class="todoItems"> <!-- unordered lsit -->
    <% for(let i=0; i < items.length; i++) {%>
        <li class="item">
            <% if(items[i].completed === true) {%> <!-- bad bad JS ; io for a loop for each item in -->
                <span class='completed'><%= items[i].thing %></span> <!-- is it completed to do this -->
            <% }else{ %>
                <span><%= items[i].thing %></span> <!-- display in the list but don't add the class-->
            <span class='fa fa-trash'></span> <!-- we gonna make click this trash can icon gets added on to each item on our list-->
        </li>
    <% } %> <!--ejs tags are weird as hell sis -->
    </ul>

    <h2>Left to do: <%= left %></h2> <!-- count out number of incomplete tasks and shove here-->

    <h2>Add A Todo:</h2> <!-- less important -->

    <form action="/addTodo" method="POST"> <!-- redirect to here once we submit-->
        <input type="text" placeholder="Thing To Do" name="todoItem"> <!-- march to line 40 in our server.js file if you forget-->
        <input type="submit">
    </form>
    

    <script src='js/main.js'></script> <!-- run some JS - put it at bottom so it load last adn doesn't mess up the cute css/html rendering -->
</body>
</html>
